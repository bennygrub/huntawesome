<%= form_for(@task_documentation) do |f| %>
  <% if @task_documentation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_documentation.errors.count, "error") %> prohibited this task_documentation from being saved:</h2>

      <ul>
      <% @task_documentation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
